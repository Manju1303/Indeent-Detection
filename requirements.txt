# ============================================================
# Open-Source Theft & Violence Detection System
# requirements.txt — All packages are FREE and open-source
# ============================================================

# Core Deep Learning
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Object Detection
ultralytics>=8.0.0          # YOLOv8 (AGPL-3.0)

# Computer Vision
opencv-python>=4.8.0
opencv-contrib-python>=4.8.0

# Pose Estimation
mediapipe>=0.10.0           # Google (Apache 2.0)

# Tracking
# ByteTrack is included inline (no pip package needed)
scipy>=1.11.0               # Used by ByteTrack internally
lap>=0.4.0                  # Linear assignment (ByteTrack)

# Numerical
numpy>=1.24.0
Pillow>=10.0.0

# Web Dashboard
flask>=3.0.0
flask-socketio>=5.3.0       # Real-time updates

# Database
# SQLite3 is built into Python — no install needed

# Alert System
requests>=2.31.0            # Telegram Bot API calls
python-telegram-bot>=20.0   # Telegram (optional wrapper)

# Configuration
pyyaml>=6.0.1

# Utilities
tqdm>=4.66.0                # Training progress bars
matplotlib>=3.7.0           # Training charts
seaborn>=0.12.0

# Audio alerts (local buzzer)
playsound>=1.3.0

# Email alerts
# smtplib is built into Python — no install needed

# Performance
psutil>=5.9.0               # System resource monitoring
